# API服务配置
upstream api_backend {
    server app:3000;
    keepalive 32;
}

# HTTP配置 (生产环境 - 80端口)
server {
    listen 80;
    # 绑定你的域名
    server_name pawstep.fun www.pawstep.fun localhost;
    
    # 如果已配置 SSL，建议取消下面注释开启强制 HTTPS 跳转
    # return 301 https://$host$request_uri;
    
    # 暂时在 80 端口提供服务
    location / {
        proxy_pass http://api_backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 300s;
        proxy_connect_timeout 75s;
    }

    location /api-docs {
        proxy_pass http://api_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }
}

# HTTPS配置 (预留，需要证书文件)
# server {
#     listen 443 ssl http2;
#     server_name pawstep.fun www.pawstep.fun;
#     
#     # SSL证书路径 (需要挂载到容器内)
#     ssl_certificate /etc/nginx/ssl/pawstep.fun.pem;
#     ssl_certificate_key /etc/nginx/ssl/pawstep.fun.key;
#     
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-RSA-AES128-SHA256:ECDHE-RSA-AES256-SHA384;
#     
#     location / {
#         proxy_pass http://api_backend;
#         # ... (同上)
#     }
# }

